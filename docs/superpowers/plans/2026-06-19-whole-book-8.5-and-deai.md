# 整本8.5达标 + 正文去AI味 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 通过大纲层伏笔闭环门禁（Part A）和正文去AI味检测（Part C），把整本终审分从 ~5.8 提升到 ≥8.5，同时保证逐章均分 ≥8.5。

**Architecture:** Part A 新增持久化伏笔台账 + writer 前的硬门禁审计脚本，强制回收 dangling 线程（直击 foreshadowing_payoff/world_consistency/character_arc 失分）。Part C 新增确定性本地 AI 味检测器，注入 reviewer 证据并触发 writer 定向重写。两者独立，先 A 后 C。

**Tech Stack:** Python 3.12, pathlib, 现有 `core/mmx_client.call_mmx`、`core/workflow_state.atomic_write_json`、`core/json_repair`。无测试框架（仓库无 pytest，按 AGENTS.md 用 `py_compile` + 自包含验证脚本）。

**Spec:** `docs/superpowers/specs/2026-06-19-whole-book-8.5-and-deai-design.md`

**关键既有 API（直接复用，勿重造）：**
- `core/workflow_state.py`: `outline_chapter_path(base, ch) -> Path`, `list_outline_chapters(base) -> list[dict]`, `atomic_write_json(path, data)`, `report_path(base, name) -> Path`
- `core/mmx_client.py`: `call_mmx(system, user, *, model, mmx_path, max_tokens, temperature, retries, retry_delay, log_dir, raw_name, qps, rate_state_dir) -> str`
- `core/novel_config.py`: `load_config(project) -> dict`, `resolve_project_dir(arg) -> Path`
- `core/json_repair.py`: `fix_inner_quotes`, `fix_truncated_json`

**测试约定（本仓库无 pytest）：** 每个 `core/` 纯逻辑模块附带 `if __name__ == "__main__"` 自检块，用 `python scripts/core/<mod>.py` 跑断言。管线脚本用 `python -m py_compile` 校验语法。

---

## Part A — 大纲层结构闭环

### Task A1: 伏笔台账模块 `scripts/core/foreshadowing_ledger.py`

**Files:**
- Create: `scripts/core/foreshadowing_ledger.py`

台账读写 + 线程登记/回收/dangling 查询的纯逻辑模块。无 API 调用，可独立自检。

- [ ] **Step 1: 创建模块骨架与常量**

```python
#!/usr/bin/env python3
"""全书级伏笔台账：登记、回收、dangling 查询。

台账文件位置：projects/<book>/reports/foreshadowing_ledger.json
foreshadowing 字段约定（机器可解析）：
  埋设：[埋]描述文字（F00X）
  回收：[收]F00X 回收说明
"""
from __future__ import annotations

import re
import time
from pathlib import Path
from typing import Any

from core.workflow_state import atomic_write_json, report_path

LEDGER_FILENAME = "foreshadowing_ledger.json"

# 伏笔类别 → 最迟回收偏移（占全书总章数比例）
CATEGORY_RESOLVE_RATIO = {
    "character_internal": 0.40,   # 角色内心线：埋设后 40% 章数内回收
    "plot_mystery": 0.50,          # 剧情谜团：中点前回收
    "world_rule": 0.95,            # 世界观规则：finale 前回收
    "power_system": 0.60,          # 力量体系：60% 内回收
    "relationship": 0.25,          # 关系线：快速回收
}
DEFAULT_CATEGORY = "plot_mystery"
DEFAULT_PRIORITY = "major"
VALID_PRIORITIES = {"critical", "major", "minor"}
VALID_STATUSES = {"planted", "planned_resolution", "resolved", "dangling"}
# 匹配 foreshadowing 字段中的 [埋]/[收] 标记与伏笔 id
PLANT_RE = re.compile(r"\[埋\].*?\(?\s*(F\d{3,})\s*\)?", re.S)
RESOLVE_RE = re.compile(r"\[收\]\s*(F\d{3,})\s*(.*)", re.S)
THREAD_ID_RE = re.compile(r"^F(\d{3,})$")


def ledger_path(project: Path) -> Path:
    return report_path(project, LEDGER_FILENAME)


def empty_ledger(book_id: str = "", total_chapters: int = 0) -> dict:
    return {
        "version": 1,
        "book_id": book_id,
        "total_chapters": total_chapters,
        "threads": [],
        "stats": {"total": 0, "resolved": 0, "dangling": 0, "critical_dangling": 0},
        "updated_at": time.strftime("%Y-%m-%d %H:%M:%S"),
    }


def load_ledger(project: Path) -> dict:
    path = ledger_path(project)
    if not path.exists():
        return empty_ledger()
    try:
        import json
        data = json.loads(path.read_text(encoding="utf-8"))
        return data if isinstance(data, dict) and "threads" in data else empty_ledger()
    except Exception:
        return empty_ledger()


def save_ledger(project: Path, ledger: dict) -> None:
    recompute_stats(ledger)
    ledger["updated_at"] = time.strftime("%Y-%m-%d %H:%M:%S")
    atomic_write_json(ledger_path(project), ledger)
```

- [ ] **Step 2: 实现 next_id / register / resolve / recompute_stats**

```python
def _next_thread_id(ledger: dict) -> str:
    max_num = 0
    for t in ledger.get("threads", []):
        m = THREAD_ID_RE.match(str(t.get("id", "")))
        if m:
            max_num = max(max_num, int(m.group(1)))
    return f"F{max_num + 1:03d}"


def compute_must_resolve_by(planted_at: int, total_chapters: int,
                            category: str = DEFAULT_CATEGORY) -> int:
    ratio = CATEGORY_RESOLVE_RATIO.get(category, CATEGORY_RESOLVE_RATIO[DEFAULT_CATEGORY])
    offset = max(1, round(total_chapters * ratio))
    return min(planted_at + offset, total_chapters)


def register_thread(ledger: dict, *, planted_at: int, setup: str,
                    category: str = DEFAULT_CATEGORY, priority: str = DEFAULT_PRIORITY,
                    total_chapters: int | None = None,
                    must_resolve_by: int | None = None) -> str:
    if category not in CATEGORY_RESOLVE_RATIO:
        category = DEFAULT_CATEGORY
    if priority not in VALID_PRIORITIES:
        priority = DEFAULT_PRIORITY
    setup = str(setup or "").strip()
    if not setup:
        raise ValueError("setup 不能为空")
    tc = total_chapters if total_chapters is not None else ledger.get("total_chapters", 0)
    if must_resolve_by is None:
        must_resolve_by = compute_must_resolve_by(planted_at, tc, category)
    thread_id = _next_thread_id(ledger)
    ledger.setdefault("threads", []).append({
        "id": thread_id,
        "planted_at": planted_at,
        "setup": setup,
        "category": category,
        "priority": priority,
        "status": "planted",
        "must_resolve_by": must_resolve_by,
        "resolved_at": None,
        "resolution_plan": "",
        "resolved_in_outline": False,
    })
    return thread_id


def resolve_thread(ledger: dict, thread_id: str, *, resolved_at: int,
                   resolution: str = "", resolved_in_outline: bool = True) -> bool:
    for t in ledger.get("threads", []):
        if t.get("id") == thread_id and t.get("status") != "resolved":
            t["status"] = "resolved"
            t["resolved_at"] = resolved_at
            t["resolution_plan"] = str(resolution or "").strip()
            t["resolved_in_outline"] = bool(resolved_in_outline)
            return True
    return False


def recompute_stats(ledger: dict) -> dict:
    threads = ledger.get("threads", [])
    total = len(threads)
    resolved = sum(1 for t in threads if t.get("status") == "resolved")
    dangling = total - resolved
    critical_dangling = sum(
        1 for t in threads
        if t.get("status") != "resolved" and t.get("priority") == "critical"
    )
    ledger["stats"] = {
        "total": total, "resolved": resolved,
        "dangling": dangling, "critical_dangling": critical_dangling,
    }
    return ledger["stats"]


def dangling_threads(ledger: dict, *, as_of_chapter: int | None = None) -> list[dict]:
    """返回所有未回收的线程；若给 as_of_chapter，进一步要求已过 must_resolve_by。"""
    out = []
    for t in ledger.get("threads", []):
        if t.get("status") == "resolved":
            continue
        if as_of_chapter is not None:
            mrb = int(t.get("must_resolve_by", 0) or 0)
            if mrb and as_of_chapter < mrb:
                continue
        out.append(t)
    return out
```

- [ ] **Step 3: 实现 foreshadowing 字段解析（`[埋]/[收]` 约定）**

```python
def parse_foreshadowing_field(text: str) -> dict:
    """解析一章 foreshadowing 字段，返回 {planted: [(id,setup)], resolved: [(id,note)]}。

    兼容无标记的纯文本（视为一条无 id 的埋设，id 留空）。
    """
    text = str(text or "")
    planted: list[tuple[str, str]] = []
    resolved: list[tuple[str, str]] = []
    for m in PLANT_RE.finditer(text):
        raw = m.group(0)
        tid = m.group(1)
        setup = re.sub(r"^\[埋\]", "", raw).strip()
        setup = re.sub(r"\(?\s*" + re.escape(tid) + r"\s*\)?\s*$", "", setup).strip()
        planted.append((tid, setup or tid))
    for m in RESOLVE_RE.finditer(text):
        resolved.append((m.group(1), m.group(2).strip()))
    has_marker = bool(PLANT_RE.search(text) or RESOLVE_RE.search(text))
    if not has_marker and text.strip():
        planted.append(("", text.strip()[:200]))
    return {"planted": planted, "resolved": resolved, "has_marker": has_marker}
```

- [ ] **Step 4: 自检块（仓库无 pytest，用 main 自检）**

```python
def _selftest() -> None:
    led = empty_ledger("test", total_chapters=60)
    f1 = register_thread(led, planted_at=7, setup="战无双杀气失控",
                         category="character_internal", priority="critical")
    assert f1 == "F001", f1
    assert led["threads"][0]["must_resolve_by"] == 7 + round(60 * 0.40)
    f2 = register_thread(led, planted_at=12, setup="第四元缺陷", category="plot_mystery")
    assert f2 == "F002"
    assert dangling_threads(led) == 2
    assert dangling_threads(led, as_of_chapter=5) == 0  # 都没过最迟章
    assert dangling_threads(led, as_of_chapter=60) == 2
    ok = resolve_thread(led, "F001", resolved_at=26, resolution="失控被稳定")
    assert ok and led["threads"][0]["status"] == "resolved"
    recompute_stats(led)
    assert led["stats"] == {"total": 2, "resolved": 1, "dangling": 1, "critical_dangling": 0}
    parsed = parse_foreshadowing_field("[埋]战无双杀气失控（F007）\n[收]F003 身世揭晓——他是混沌投射")
    assert parsed["planted"] == [("F007", "战无双杀气失控")], parsed["planted"]
    assert parsed["resolved"] == [("F003", "身世揭晓——他是混沌投射")], parsed["resolved"]
    print("foreshadowing_ledger selftest OK")


if __name__ == "__main__":
    _selftest()
```

- [ ] **Step 5: 运行自检 + py_compile**

Run: `python scripts/core/foreshadowing_ledger.py`
Expected: `foreshadowing_ledger selftest OK`

Run: `python -m py_compile scripts/core/foreshadowing_ledger.py`
Expected: 无输出（语法通过）

- [ ] **Step 6: Commit**

```bash
git add scripts/core/foreshadowing_ledger.py
git commit -m "feat(foreshadowing): 全书伏笔台账模块（登记/回收/dangling查询）"
```

---

### Task A2: 台账重建器 — 从全书大纲扫描重建台账

**Files:**
- Create: `scripts/core/foreshadowing_ledger.py`（追加 `rebuild_from_outlines` 函数）

把扫描逻辑放在 core 模块（纯逻辑、可自检），audit 脚本只做编排和 API 调用。

- [ ] **Step 1: 追加 rebuild_from_outlines 函数**

在 `foreshadowing_ledger.py` 末尾（`_selftest` 之前）追加：

```python
def rebuild_from_outlines(project: Path, *, book_id: str = "",
                          total_chapters: int | None = None) -> dict:
    """扫描全书大纲，按章节顺序重建台账。

    - 遍历 chapter_XXXX.json，解析 foreshadowing 字段
    - [埋] 标记（带 id）→ register_thread（已存在同 id 则跳过）
    - [收] 标记 → resolve_thread
    - 无 id 的纯文本埋设 → 用 planted_at+setup 生成新 id
    所有未回收线程标 status=dangling（由 recompute_stats 统计）
    """
    from core.workflow_state import list_outline_chapters
    chapters = list_outline_chapters(project)
    tc = total_chapters or (len(chapters))
    ledger = empty_ledger(book_id or project.name, total_chapters=tc)
    chapters.sort(key=lambda c: int(c.get("chapter_number", 0) or 0))
    for ch in chapters:
        num = int(ch.get("chapter_number", 0) or 0)
        if num <= 0:
            continue
        parsed = parse_foreshadowing_field(ch.get("foreshadowing", ""))
        for tid, setup in parsed["planted"]:
            existing = next((t for t in ledger["threads"] if t.get("id") == tid), None)
            if existing:
                continue
            register_thread(
                ledger, planted_at=num, setup=setup or f"第{num}章伏笔",
                category=DEFAULT_CATEGORY, priority=DEFAULT_PRIORITY,
                total_chapters=tc,
            )
        for tid, note in parsed["resolved"]:
            if tid and any(t.get("id") == tid for t in ledger["threads"]):
                resolve_thread(ledger, tid, resolved_at=num, resolution=note)
    # 标记 dangling：所有未 resolved 的，若已过 must_resolve_by
    for t in ledger["threads"]:
        if t.get("status") != "resolved":
            mrb = int(t.get("must_resolve_by", 0) or 0)
            t["status"] = "dangling" if mrb and tc >= mrb else t.get("status", "planted")
    recompute_stats(ledger)
    return ledger
```

- [ ] **Step 2: 扩展自检覆盖 rebuild**

在 `_selftest` 末尾（`print` 之前）追加：

```python
    import tempfile, json
    with tempfile.TemporaryDirectory() as td:
        proj = Path(td)
        odir = proj / "chapters" / "outline"
        odir.mkdir(parents=True)
        for num, foreshadow in [
            (7, "[埋]战无双杀气失控（F101）"),
            (12, "[埋]第四元缺陷（F102）"),
            (26, "[收]F101 杀气失控被姬雪潇稳定"),
        ]:
            (odir / f"chapter_{num:04d}.json").write_text(
                json.dumps({"chapter_number": num, "foreshadowing": foreshadow},
                           ensure_ascii=False), encoding="utf-8")
        led2 = rebuild_from_outlines(proj, total_chapters=60)
        ids = {t["id"]: t for t in led2["threads"]}
        assert ids["F101"]["status"] == "resolved", ids["F101"]
        assert ids["F102"]["status"] == "dangling", ids["F102"]
        assert led2["stats"]["dangling"] == 1, led2["stats"]
```

- [ ] **Step 3: 运行自检**

Run: `python scripts/core/foreshadowing_ledger.py`
Expected: `foreshadowing_ledger selftest OK`

- [ ] **Step 4: Commit**

```bash
git add scripts/core/foreshadowing_ledger.py
git commit -m "feat(foreshadowing): 台账从全书大纲重建（rebuild_from_outlines）"
```

---

### Task A3: 全书闭环审计脚本 `scripts/maintenance/foreshadowing_audit.py`

**Files:**
- Create: `scripts/maintenance/foreshadowing_audit.py`

编排：重建台账 → 检出 dangling → 生成回收补丁（LLM）→ 回写大纲 → 门禁判定。退出码 0=通过，1=dangling 超阈值。

- [ ] **Step 1: 创建脚本骨架（init/log/config/ai_call）**

```python
#!/usr/bin/env python3
"""全书伏笔闭环审计：writer 前的硬门禁。

流程：重建台账 → 检出 dangling → 生成回收补丁（LLM）→ 回写大纲 → 门禁判定。
退出码：0=通过（无 critical dangling），1=dangling 超阈值。
"""
from __future__ import annotations

import argparse
import json
import os
import sys
import time
from pathlib import Path

TOOLS_ROOT = Path(__file__).resolve().parents[1]
if str(TOOLS_ROOT) not in sys.path:
    sys.path.insert(0, str(TOOLS_ROOT))

from core.mmx_client import MmxError, call_mmx
from core.novel_config import load_config, resolve_project_dir
from core.workflow_state import atomic_write_json, outline_chapter_path
from core.json_repair import fix_inner_quotes, fix_truncated_json
from core.foreshadowing_ledger import (
    dangling_threads, ledger_path, rebuild_from_outlines, resolve_thread, save_ledger,
)


def log(project: Path, msg: str) -> None:
    line = f"[{time.strftime('%Y-%m-%d %H:%M:%S')}] [ForeshadowingAudit] {msg}"
    print(line, flush=True)
    p = project / "logs" / "foreshadowing_audit.log"
    p.parent.mkdir(parents=True, exist_ok=True)
    with open(p, "a", encoding="utf-8") as f:
        f.write(line + "\n")


def _parse_json(raw: str) -> dict:
    text = raw.strip()
    if "```json" in text:
        text = text.split("```json", 1)[1].split("```", 1)[0].strip()
    elif "```" in text:
        text = text.split("```", 1)[1].split("```", 1)[0].strip()
    for cand in (text, fix_inner_quotes(text), fix_truncated_json(text)):
        try:
            data = json.loads(cand)
            if isinstance(data, dict):
                return data
        except Exception:
            continue
    return {"status": "parse_error", "raw_response": raw[:3000]}


def ai_call(project: Path, config: dict, prompt: str, raw_name: str) -> dict:
    cfg = config.get("foreshadowing_audit", {})
    try:
        raw = call_mmx(
            "你是长篇小说伏笔回收专家。只依据输入证据，输出合法紧凑JSON，不使用Markdown。",
            prompt,
            model=config["model"],
            mmx_path=config["mmx_path"],
            max_tokens=int(cfg.get("max_tokens", 2048)),
            temperature=float(cfg.get("temperature", 0.3)),
            retries=int(cfg.get("retries", 2)),
            retry_delay=float(cfg.get("retry_delay", 5.0)),
            timeout=int(cfg.get("timeout_seconds", 180)),
            log_dir=project / "logs" / "raw_responses",
            raw_name=raw_name,
            qps=float(config.get("api_qps", 5.0)),
            rate_state_dir=project / "logs" / "rate_limit",
        )
    except MmxError as exc:
        return {"status": "failed", "error": str(exc)}
    result = _parse_json(raw)
    result.setdefault("status", "completed")
    return result
```

- [ ] **Step 2: 实现回收补丁生成 prompt 与函数**

```python
def generate_resolution_patch(project: Path, config: dict, thread: dict,
                              total_chapters: int) -> dict:
    """对单条 dangling 线程生成回收补丁。"""
    target = min(int(thread.get("must_resolve_by", total_chapters) or total_chapters),
                 total_chapters)
    setup = thread.get("setup", "")
    prompt = f"""以下伏笔在大纲中埋下但未回收，请在指定章节大纲中插入回收情节。
【不得改动已有主线】，只在指定章节的 key_events（追加1-2条）或 foreshadowing 字段增量补内容。

待回收伏笔：
- id: {thread.get('id')}
- 埋设于第{thread.get('planted_at')}章：{setup}
- 类别: {thread.get('category')}，优先级: {thread.get('priority')}

指定回收章：第{target}章

要求：
1. 回收必须自然融入该章已有剧情，不能突兀
2. 回收要制造情感或信息回报，禁止"他终于明白了"式一笔带过
3. foreshadowing_update 必须使用 [收]FXXX 说明 格式

只输出JSON：
{{"thread_id":"{thread.get('id')}","target_chapter":{target},
"key_events_addition":["新增关键事件，30-60字"],
"foreshadowing_update":"[收]{thread.get('id')} 回收说明（30-80字）",
"resolution_summary":"50字内回收说明"}}"""
    return ai_call(project, config, prompt, f"foreshadow_patch_{thread.get('id')}")
```

- [ ] **Step 3: 实现补丁回写大纲函数**

```python
def apply_patch(project: Path, patch: dict) -> bool:
    """把补丁增量写回 target_chapter 的大纲 JSON。返回是否成功。"""
    target = int(patch.get("target_chapter", 0) or 0)
    if target <= 0:
        return False
    path = outline_chapter_path(project, target)
    if not path.exists():
        return False
    try:
        outline = json.loads(path.read_text(encoding="utf-8"))
    except Exception:
        return False
    additions = patch.get("key_events_addition", [])
    if isinstance(additions, list):
        events = outline.get("key_events", [])
        if not isinstance(events, list):
            events = []
        for ev in additions:
            if str(ev).strip() and str(ev).strip() not in events:
                events.append(str(ev).strip())
        outline["key_events"] = events
    update = str(patch.get("foreshadowing_update", "")).strip()
    if update:
        existing = str(outline.get("foreshadowing", "")).strip()
        outline["foreshadowing"] = (existing + "\n" + update).strip() if existing else update
    atomic_write_json(path, outline)
    return True


def patch_outline_from_thread(project: Path, config: dict, thread: dict,
                              total_chapters: int) -> bool:
    """生成并应用一条回收补丁。成功返回 True。"""
    patch = generate_resolution_patch(project, config, thread, total_chapters)
    if patch.get("status") != "completed":
        return False
    if not apply_patch(project, patch):
        return False
    # 标记台账已回收（resolved_in_outline=True）
    tid = thread.get("id")
    ledger_path_file = ledger_path(project)
    if ledger_path_file.exists():
        ledger = json.loads(ledger_path_file.read_text(encoding="utf-8"))
        resolve_thread(ledger, tid, resolved_at=int(patch.get("target_chapter", 0) or 0),
                       resolution=patch.get("resolution_summary", ""), resolved_in_outline=True)
        save_ledger(project, ledger)
    return True
```

- [ ] **Step 4: 实现主流程 main（重建→门禁→可选补丁→重判）**

```python
def run_audit(project: Path, config: dict, *, apply_patches: bool,
              max_rounds: int, block_threshold: int) -> dict:
    total = int(config.get("total_chapters", 0))
    ledger = rebuild_from_outlines(project, book_id=project.name, total_chapters=total)
    save_ledger(project, ledger)
    log(project, f"台账重建完成: {ledger['stats']}")

    dangling = dangling_threads(ledger)
    critical = [t for t in dangling if t.get("priority") == "critical"]
    log(project, f"dangling={len(dangling)} critical={len(critical)}")

    applied = 0
    if apply_patches and dangling:
        for rnd in range(1, max_rounds + 1):
            still = dangling_threads(ledger)
            if not still:
                break
            log(project, f"补丁轮 {rnd}/{max_rounds}，待处理 {len(still)} 条")
            for t in still:
                if patch_outline_from_thread(project, config, t, total):
                    applied += 1
            # 重建台账复判
            ledger = rebuild_from_outlines(project, book_id=project.name, total_chapters=total)
            save_ledger(project, ledger)

    ledger = rebuild_from_outlines(project, book_id=project.name, total_chapters=total)
    save_ledger(project, ledger)
    final_critical = sum(1 for t in ledger["threads"]
                         if t.get("status") != "resolved" and t.get("priority") == "critical")
    passed = final_critical <= block_threshold
    result = {
        "status": "completed",
        "stats": ledger["stats"],
        "critical_dangling": final_critical,
        "patches_applied": applied,
        "block_threshold": block_threshold,
        "gate_passed": passed,
        "ledger_file": str(ledger_path(project)),
    }
    report = project / "reports" / "foreshadowing_audit_report.json"
    atomic_write_json(report, result)
    log(project, f"审计完成 gate_passed={passed} {ledger['stats']}")
    return result


def main() -> int:
    parser = argparse.ArgumentParser(description="全书伏笔闭环审计（writer 前门禁）")
    parser.add_argument("--project", "-p", default=os.getenv("NOVEL_PROJECT_DIR", ""))
    parser.add_argument("--force", action="store_true", help="忽略缓存，全量重建")
    parser.add_argument("--apply-patches", action="store_true",
                        help="自动生成并应用回收补丁（默认只报告）")
    parser.add_argument("--max-rounds", type=int, default=3, help="补丁重试上限")
    parser.add_argument("--block-threshold", type=int, default=0,
                        help="critical dangling 阻断阈值（默认0）")
    args = parser.parse_args()
    project = resolve_project_dir(args.project)
    config = load_config(project)
    result = run_audit(
        project, config,
        apply_patches=args.apply_patches,
        max_rounds=args.max_rounds,
        block_threshold=args.block_threshold,
    )
    return 0 if result["gate_passed"] else 1


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 5: py_compile + 语法校验**

Run: `python -m py_compile scripts/maintenance/foreshadowing_audit.py`
Expected: 无输出

- [ ] **Step 6: 用 novels13 做 dry-run 只读校验（不应用补丁）**

Run: `python scripts/maintenance/foreshadowing_audit.py --project projects/novels13`
Expected: 退出码 1（novels13 有已知 dangling：战无双杀气失控等），日志打印 dangling 数 ≥1，生成 `reports/foreshadowing_ledger.json` 与 `reports/foreshadowing_audit_report.json`，`gate_passed=false`。这验证了审计能检出 novels13 终审报告里点名的真实问题。

- [ ] **Step 7: Commit**

```bash
git add scripts/maintenance/foreshadowing_audit.py
git commit -m "feat(audit): 全书伏笔闭环审计门禁（dangling检测+LLM回收补丁）"
```

---

### Task A4: outliner 注入台账约束 + `[埋]/[收]` 约定

**Files:**
- Modify: `scripts/pipeline/outliner.py`（prompt 注入 + 写后台账更新钩子）

让 outliner 生成每章时"看见"应回收的线程，并在写入后解析 foreshadowing 字段更新台账。

- [ ] **Step 1: 在 outliner.py 顶部导入台账模块**

在 `outliner.py` 的现有 `from core.outline_memory import ...` 之后追加：

```python
from core.foreshadowing_ledger import (
    dangling_threads as ledger_dangling,
    load_ledger,
    parse_foreshadowing_field,
    register_thread,
    resolve_thread,
    save_ledger,
)
```

- [ ] **Step 2: 新增台账约束注入函数**

在 outliner.py 中（`def call_mmx` 之后）追加：

```python
def _ledger_resolve_directive(batch_start: int) -> str:
    """生成第 batch_start 章应回收的伏笔清单（注入 prompt）。无则返回空串。"""
    try:
        ledger = load_ledger(NOVELS_DIR)
        due = ledger_dangling(ledger, as_of_chapter=batch_start)
    except Exception:
        return ""
    if not due:
        return ""
    lines = ["## 【本章必须回收的伏笔】（强制要求，未回收视为不合格）"]
    for t in due[:8]:
        lines.append(
            f"- {t.get('id')}（第{t.get('planted_at')}章埋下，类别{t.get('category')}，"
            f"优先级{t.get('priority')}）：{t.get('setup', '')}"
        )
    lines.append("本章 foreshadowing 字段必须用 [收]FXXX 回收说明 格式回收至少一条。"
                 "埋设新伏笔用 [埋]描述（FXXX）格式。")
    return "\n".join(lines)
```

- [ ] **Step 3: 在批量生成 prompt 中注入约束（含配角弧线检查）**

定位 `outliner.py` 中组装批量 prompt 的位置（含 `story_beat 必须取枚举值` 的 prompt 字符串）。在该 prompt 末尾、最终要求之前插入台账约束与配角弧线指令：

```python
        + _ledger_resolve_directive(batch_start)
        + "\n\n## 【配角弧线规划】（A3）\n"
        + "为每个本章涉及的有名配角（见角色信息）落实弧线推进：若该配角正处于转折/高潮/收束节点，"
          "本章 key_events 必须包含其弧线推进事件，禁止配角出场后连续多章消失或沦为背景板。"
```

（用字符串拼接，与现有 `+ "\n"` 风格一致。若该 prompt 是 f-string，则在合适位置嵌入 `{_ledger_resolve_directive(batch_start)}`。）

> **Spec 对应：** 覆盖 spec §3.3 配角弧线规划注入的轻量版——通过 prompt 指令约束 outliner 关注配角弧线，而非引入结构化 character_arc_plan 字段（后者改动大且 spec §9 YAGNI 精神建议克制）。最终整本层面由 book_reviewer 的 character_arc 维度验收。

- [ ] **Step 4: 新增写后台账更新钩子**

在 outliner.py 中（`write_outline_chapters(...)` 调用之后、解析失败处理之前）追加：

```python
            # 台账更新钩子：解析新生成章节的 foreshadowing 字段
            try:
                _update_ledger_from_new_chapters(new_chapters)
            except Exception as _le:
                print(f"[Outliner] 台账更新失败（不影响大纲）: {_le}")
```

并实现该函数（放在 `_ledger_resolve_directive` 之后）：

```python
def _update_ledger_from_new_chapters(chapters: list) -> None:
    ledger = load_ledger(NOVELS_DIR)
    tc = int(CONFIG.get("total_chapters", 0)) or ledger.get("total_chapters", 0)
    ledger["total_chapters"] = tc or ledger.get("total_chapters", 0)
    changed = False
    for ch in chapters:
        num = int(ch.get("chapter_number", 0) or 0)
        if num <= 0:
            continue
        parsed = parse_foreshadowing_field(ch.get("foreshadowing", ""))
        for tid, setup in parsed["planted"]:
            if tid and any(t.get("id") == tid for t in ledger["threads"]):
                continue
            register_thread(ledger, planted_at=num, setup=setup or f"第{num}章伏笔",
                            total_chapters=tc)
            changed = True
        for tid, note in parsed["resolved"]:
            if tid and resolve_thread(ledger, tid, resolved_at=num, resolution=note):
                changed = True
    if changed:
        save_ledger(NOVELS_DIR, ledger)
```

- [ ] **Step 5: py_compile 全管线脚本**

Run: `python -m py_compile scripts/pipeline/outliner.py scripts/core/foreshadowing_ledger.py`
Expected: 无输出

- [ ] **Step 6: Commit**

```bash
git add scripts/pipeline/outliner.py
git commit -m "feat(outliner): 注入伏笔台账约束 + 写后台账更新钩子（[埋]/[收]约定）"
```

---

### Task A5: coordinator 集成 foreshadowing_audit 门禁

**Files:**
- Modify: `scripts/pipeline/coordinator.py`

在 outline 阶段全部完成后、writer 之前插入门禁调用。

- [ ] **Step 1: 在 MAINTENANCE_SCRIPTS 确认 audit 脚本路径**

coordinator.py 顶部已有：
```python
MAINTENANCE_SCRIPTS = {
    "wechat_pusher_lane.py": ...,
    "gate_watchdog.py": ...,
    "outline_book_reviewer.py": ...,
}
```
追加一项：
```python
    "foreshadowing_audit.py": TOOLS_ROOT / "maintenance" / "foreshadowing_audit.py",
```

- [ ] **Step 2: 在 writer 阶段入口前调用门禁**

定位 coordinator.py 中 writer 阶段开始的位置（搜索调用 `writer.py` 的阶段函数或 run_script）。在该阶段函数开头插入：

```python
        # A2 门禁：伏笔闭环审计（writer 前硬门禁）
        audit_script = TOOLS_ROOT / "maintenance" / "foreshadowing_audit.py"
        rc = self.run_streaming_process(
            [sys.executable, str(audit_script), "--project", str(NOVELS_DIR),
             "--apply-patches", "--block-threshold", "0"],
            child_log=LOGS_DIR / "foreshadowing_audit.log",
        )
        if rc != 0:
            self.log("[Coordinator] 伏笔闭环门禁未通过，阻断 writer 阶段")
            return False
```

（若 coordinator 用 `run_script` 而非 `run_streaming_process`，则改用 `self.run_script` 等价调用，保持与现有 outline_book_reviewer 调用风格一致。）

- [ ] **Step 3: py_compile**

Run: `python -m py_compile scripts/pipeline/coordinator.py`
Expected: 无输出

- [ ] **Step 4: Commit**

```bash
git add scripts/pipeline/coordinator.py
git commit -m "feat(coordinator): writer 前集成伏笔闭环门禁（foreshadowing_audit）"
```

---

## Part C — 正文去AI味

### Task C1: AI味词表 `scripts/core/ai_flavor_lexicon.json`

**Files:**
- Create: `scripts/core/ai_flavor_lexicon.json`

可被项目级 `projects/<book>/ai_flavor_lexicon.json` 覆盖。

- [ ] **Step 1: 写默认词表**

```json
{
  "parallel_markers": ["不仅", "而且", "更", "既", "又", "一方面", "另一方面", "与其说", "不如说"],
  "parallel_patterns": [
    "不仅.{1,40}而且.{1,40}更",
    "只要.{1,30}就.{1,30}只要.{1,30}就",
    "既.{1,30}又.{1,30}还",
    ".{2,15}的.{2,15}的.{2,15}的.{2,15}的"
  ],
  "summary_ending_phrases": [
    "故事才刚刚开始", "就这样", "他明白了", "一切都有了答案", "从此以后",
    "这便是", "他知道，这不会是最后一次", "命运的齿轮", "新的一天", "未完待续"
  ],
  "adjective_pileup": [
    "璀璨", "磅礴", "恐怖", "惊天", "震撼", "无与伦比", "亘古", "浩瀚",
    "不可思议", "惊天动地", "惊天动魄", "无以复加", "前所未有", "前所未见"
  ],
  "meta_narration": [
    "作为AI", "以下是正文", "本章完", "字数：", "字数:", "生成完毕", "未完待续"
  ],
  "telling_verbs": ["他感到", "她感到", "他意识到", "她意识到", "他明白了", "她明白了", "心中涌起"]
}
```

- [ ] **Step 2: Commit**

```bash
git add scripts/core/ai_flavor_lexicon.json
git commit -m "feat(deai): AI味检测默认词表（排比/收尾/形容词/元叙述/告知动词）"
```

---

### Task C2: AI味检测器 `scripts/core/ai_flavor_detector.py`

**Files:**
- Create: `scripts/core/ai_flavor_detector.py`

确定性检测，不耗 API。返回 score（0-10，越高越自然）+ 定位到段落的问题清单。

- [ ] **Step 1: 创建模块（加载词表 + 段落切分 + 度量）**

```python
#!/usr/bin/env python3
"""正文 AI 味确定性检测。不耗 API。

基于 StoryScope 30特征精神 + 中文网文通病：排比抒情、总结收尾、形容词堆砌、
低段落方差（过于工整）、元叙述、告知而非展示。

返回 ai_flavor_score（0-10，越高越自然）+ 定位到段落的问题清单。
词表可被 projects/<book>/ai_flavor_lexicon.json 覆盖。
"""
from __future__ import annotations

import json
import re
from pathlib import Path

DEFAULT_LEXICON = Path(__file__).resolve().parent / "ai_flavor_lexicon.json"


def load_lexicon(project: Path | None = None) -> dict:
    if project is not None:
        override = project / "ai_flavor_lexicon.json"
        if override.exists():
            try:
                return json.loads(override.read_text(encoding="utf-8"))
            except Exception:
                pass
    if DEFAULT_LEXICON.exists():
        return json.loads(DEFAULT_LEXICON.read_text(encoding="utf-8"))
    return {}


def _split_paragraphs(text: str) -> list[str]:
    return [p.strip() for p in re.split(r"\n\s*\n", text) if p.strip()]


def _count_per_kanji(text: str, hits: int) -> float:
    cjk = sum(1 for c in text if "\u4e00" <= c <= "\u9fff")
    return hits / (cjk / 1000) if cjk else 0.0
```

- [ ] **Step 2: 实现度量函数**

```python
def _detect_parallel(text: str, lex: dict) -> list[dict]:
    issues = []
    for pat in lex.get("parallel_patterns", []):
        for m in re.finditer(pat, text):
            issues.append({
                "type": "parallel_sentiment", "severity": "major",
                "evidence": m.group(0)[:60],
                "suggestion": "打破排比工整，改为长短句交替 + 具体动作",
            })
    return issues


def _detect_summary_ending(text: str, lex: dict) -> list[dict]:
    tail = text[-300:] if len(text) > 300 else text
    issues = []
    for phrase in lex.get("summary_ending_phrases", []):
        if phrase in tail:
            issues.append({
                "type": "summary_ending", "severity": "major",
                "evidence": phrase,
                "suggestion": "章末禁止总结收尾，改为未决的威胁或未说出口的话",
            })
    return issues


def _detect_adjective_pileup(text: str, lex: dict) -> list[dict]:
    hits = sum(text.count(w) for w in lex.get("adjective_pileup", []))
    density = _count_per_kanji(text, hits)
    if density >= 2.0:
        return [{
            "type": "adjective_pileup", "severity": "minor",
            "evidence": f"高频AI形容词密度 {density:.1f}/千字",
            "suggestion": "删除空泛形容词，替换为可感感官细节",
        }]
    return []


def _detect_meta(text: str, lex: dict) -> list[dict]:
    issues = []
    for phrase in lex.get("meta_narration", []):
        if phrase in text:
            issues.append({
                "type": "meta_narration", "severity": "major",
                "evidence": phrase,
                "suggestion": "删除生成痕迹/元叙述",
            })
    return issues


def _detect_telling(text: str, lex: dict) -> list[dict]:
    hits = sum(text.count(v) for v in lex.get("telling_verbs", []))
    density = _count_per_kanji(text, hits)
    if density >= 1.0:
        return [{
            "type": "telling_not_showing", "severity": "minor",
            "evidence": f"'感到/意识到'类告知密度 {density:.1f}/千字",
            "suggestion": "用动作和物象替代直接告知情绪",
        }]
    return []


def _detect_low_variance(text: str) -> list[dict]:
    paras = _split_paragraphs(text)
    if len(paras) < 6:
        return []
    lengths = [sum(1 for c in p if "\u4e00" <= c <= "\u9fff") for p in paras]
    mean = sum(lengths) / len(lengths) if lengths else 0
    if mean <= 0:
        return []
    var = sum((l - mean) ** 2 for l in lengths) / len(lengths)
    cv = (var ** 0.5) / mean  # 变异系数
    if cv < 0.4:
        return [{
            "type": "low_variance", "severity": "minor",
            "evidence": f"段落长度变异系数 {cv:.2f}（过于工整）",
            "suggestion": "穿插1-2句短段落制造停顿，再用长段落铺陈",
        }]
    return []
```

- [ ] **Step 3: 实现主函数 detect_ai_flavor + 定位到段落**

```python
def detect_ai_flavor(text: str, project: Path | None = None) -> dict:
    """检测 AI 味。返回 {ai_flavor_score, issues, metrics}。"""
    text = str(text or "")
    lex = load_lexicon(project)
    issues = (
        _detect_parallel(text, lex)
        + _detect_summary_ending(text, lex)
        + _detect_adjective_pileup(text, lex)
        + _detect_meta(text, lex)
        + _detect_telling(text, lex)
        + _detect_low_variance(text)
    )
    # 加权扣分
    penalty = 0.0
    for it in issues:
        sev = it.get("severity")
        penalty += {"major": 1.5, "minor": 0.6}.get(sev, 0.5)
    score = max(0.0, min(10.0, 10.0 - penalty))
    # 给每个 issue 定位到段落
    paras = _split_paragraphs(text)
    for it in issues:
        ev = it.get("evidence", "")
        it["paragraph"] = _locate_paragraph(paras, ev)
    return {"ai_flavor_score": round(score, 2), "issues": issues}


def _locate_paragraph(paras: list[str], evidence: str) -> str:
    if not evidence or not paras:
        return "未定位"
    for i, p in enumerate(paras, 1):
        if evidence[:20] in p:
            return f"第{i}段"
    return "未定位"
```

- [ ] **Step 4: 自检块**

```python
def _selftest() -> None:
    natural = "他站起身。窗外有风。\n\n杯子很凉。他没说话，把信折好放进抽屉。"
    r1 = detect_ai_flavor(natural)
    assert r1["ai_flavor_score"] >= 8.5, r1
    ai_heavy = (
        "苏长空不仅感受到了磅礴的力量，而且看到了璀璨的光芒，更明白了恐怖的真相。\n\n"
        "他心中涌起一股暖流。他意识到，命运亘古不变。\n\n"
        "故事才刚刚开始。"
    )
    r2 = detect_ai_flavor(ai_heavy)
    assert r2["ai_flavor_score"] < 7.0, r2
    types = {i["type"] for i in r2["issues"]}
    assert "parallel_sentiment" in types
    assert "summary_ending" in types
    assert "telling_not_showing" in types
    print("ai_flavor_detector selftest OK")


if __name__ == "__main__":
    _selftest()
```

- [ ] **Step 5: 运行自检 + py_compile**

Run: `python scripts/core/ai_flavor_detector.py`
Expected: `ai_flavor_detector selftest OK`

Run: `python -m py_compile scripts/core/ai_flavor_detector.py`
Expected: 无输出

- [ ] **Step 6: Commit**

```bash
git add scripts/core/ai_flavor_detector.py
git commit -m "feat(deai): AI味确定性检测器（排比/收尾/形容词/元叙述/方差/告知）"
```

---

### Task C3: reviewer 注入 ai_flavor 维度 + 检测证据

**Files:**
- Modify: `scripts/pipeline/reviewer.py`

把 C1 检测结果喂给 reviewer，并在 scores schema 增加 `ai_flavor`。

- [ ] **Step 1: 在 reviewer.py 导入检测器**

在 reviewer.py 现有 `from core.workflow_state import ...` 之后追加：

```python
from core.ai_flavor_detector import detect_ai_flavor
```

- [ ] **Step 2: 在 analyze_chapter_text 中加入检测证据**

定位 `review_chapter` 中 `local_analysis = analyze_chapter_text(chapter_content)` 这一行，在其后追加：

```python
    ai_flavor = detect_ai_flavor(chapter_content, project=NOVELS_DIR)
    local_analysis["ai_flavor_detection"] = ai_flavor
```

（`analyze_chapter_text` 返回 dict，直接追加 key 即可，下游 `json.dumps(local_analysis)` 会自动带上。）

- [ ] **Step 3: 在 scores JSON schema 增加 ai_flavor 维度**

定位 reviewer.py 中 `"scores": {{ ... }}` JSON 模板（含 `"read_desire": "..."` 那段）。在 `read_desire` 之后追加一行：

```python
    "ai_flavor": "去AI味程度（0-10。越高越自然。参考 local_analysis.ai_flavor_detection 的本地证据）",
```

- [ ] **Step 4: 在审查指令中强化 AI 味门槛**

定位 reviewer.py prompt 末尾的【9分神作审查清单】要求段（`要求：` 开头的编号列表）。在其中（如第10条 `必须输出 edits 数组` 之后）追加：

```python
11. 若 local_analysis.ai_flavor_detection.ai_flavor_score < 7，verdict 不得为"通过"，必须在 edits 中给出针对排比抒情/总结收尾/形容词堆砌的定点重写。
```

- [ ] **Step 5: py_compile**

Run: `python -m py_compile scripts/pipeline/reviewer.py scripts/core/ai_flavor_detector.py`
Expected: 无输出

- [ ] **Step 6: Commit**

```bash
git add scripts/pipeline/reviewer.py
git commit -m "feat(reviewer): 注入 ai_flavor 维度 + 本地检测证据，AI味重则不通过"
```

---

### Task C4: writer 去AI味重写分支

**Files:**
- Modify: `scripts/pipeline/writer.py`

当 reviewer 标记 ai_flavor 低时，走专用去AI味重写指令；重写后复检，未提升则回退。

- [ ] **Step 1: 在 writer.py 导入检测器与重写指令生成器**

在 writer.py 现有 `from core.edit_diff import ...` 之后追加：

```python
from core.ai_flavor_detector import detect_ai_flavor
```

- [ ] **Step 2: 实现去AI味重写指令生成器**

在 writer.py 中（`_writer_quality_contract` 函数之后）追加：

```python
def _deai_rewrite_directives(detection: dict) -> str:
    """根据 C1 检测结果生成针对性去AI味指令。无问题返回空串。"""
    issues = detection.get("issues", []) if isinstance(detection, dict) else []
    if not issues:
        return ""
    parts = ["## 【去AI味专项重写】（本次重写的首要目标，优先于其他修改）"]
    by_type = {}
    for it in issues:
        by_type.setdefault(it.get("type"), it)
    for t, it in by_type.items():
        loc = it.get("paragraph", "")
        loc_str = f"（定位：{loc}）" if loc and loc != "未定位" else ""
        if t == "parallel_sentiment":
            parts.append(f"- 打破排比工整{loc_str}：把'不仅…而且…更…'改为长短句交替，"
                         "用一个具体动作或物象替代抒情（'他把杯子转了三圈' > '他心中涌起波澜'）")
        elif t == "summary_ending":
            parts.append("- 章末禁止总结性收尾：最后一句必须是未解决的问题、突然的威胁或未说出口的话，"
                         "禁止'故事才刚刚开始'式元叙述")
        elif t == "adjective_pileup":
            parts.append("- 删除'璀璨/磅礴/恐怖'等空泛形容词，每个形容词替换为具体感官细节"
                         "（'空气冷得像含着铁片' > '空气无比冰冷'）")
        elif t == "low_variance":
            parts.append("- 段落长度必须有变化：穿插1-2句短段落制造停顿，再用长段落铺陈")
        elif t == "telling_not_showing":
            parts.append("- 用动作和物象替代'他感到/他意识到'式直接告知情绪")
        elif t == "meta_narration":
            parts.append("- 删除一切生成痕迹/元叙述词")
    return "\n".join(parts)
```

- [ ] **Step 3: 在重写 system prompt 中注入去AI味指令**

定位 writer.py 中 `if is_rewrite:` 分支里 `system = f"""..."""`（含"局部精修原则"那段）。在该 system 字符串末尾（`## 精修操作指南` 之后）插入：

```python
        + "\n\n"
        + _deai_rewrite_directives(
            (review_data.get("local_analysis", {}) or {}).get("ai_flavor_detection", {})
            if isinstance(review_data, dict) else {}
        )
```

- [ ] **Step 4: 增量编辑成功后复检，未提升则回退**

定位 writer.py 中 `_apply_incremental_edits(...)` 调用成功后、`return "success"` 之前（即 `log(f"[Writer] 第{chapter_number}章通过增量编辑保存..."` 附近）。在该 `return "success"` 之前插入复检：

```python
                # 去AI味复检：若重写后 ai_flavor 反而下降，回退到原文（避免越改越差）
                try:
                    old_detect = detect_ai_flavor(old_content, project=NOVELS_DIR)
                    new_detect = detect_ai_flavor(incremental_text, project=NOVELS_DIR)
                    if (new_detect["ai_flavor_score"] < old_detect["ai_flavor_score"]
                            and old_detect["ai_flavor_score"] >= 6.0):
                        log(f"[Writer] 第{chapter_number}章去AI味复检：重写后 "
                            f"{new_detect['ai_flavor_score']} < 原文 {old_detect['ai_flavor_score']}，回退保留原文")
                        incremental_text = old_content
                except Exception as _de:
                    log(f"[Writer] 去AI味复检异常（忽略）: {_de}")
```

（注：`old_content` 在该作用域已存在，是 `_apply_incremental_edits` 调用前读取的原文。）

- [ ] **Step 5: py_compile**

Run: `python -m py_compile scripts/pipeline/writer.py scripts/core/ai_flavor_detector.py`
Expected: 无输出

- [ ] **Step 6: Commit**

```bash
git add scripts/pipeline/writer.py
git commit -m "feat(writer): 去AI味重写分支（检测证据驱动 + 重写后复检回退）"
```

---

## 验证阶段

### Task V1: 创建验证项目 novels14 并跑小批量

**Files:**
- Create: `projects/novels14/`（config.json/world.json/characters.json/premise.txt）

- [ ] **Step 1: 建立 novels14 骨架（60章玄幻）**

参考 novels13 结构，创建：
- `projects/novels14/config.json`：复用 novels13 的 model/quality/reviewer 配置，`total_chapters: 60`
- `projects/novels14/world.json`：新设定（建议与 novels13 不同题材，避免污染验证）
- `projects/novels14/characters.json`：含 protagonist + ≥3 有名配角（为 A3 配角弧线提供数据）
- `projects/novels14/premise.txt`：核心设定

（可用 planner 生成，或手动建小规模设定。config 里 `total_chapters` 设 60 控制耗时。）

- [ ] **Step 2: 跑大纲阶段 + A2 门禁（先不跑 writer）**

Run:
```bash
python scripts/pipeline/outliner.py --project projects/novels14 --start 1 --end 60
python scripts/pipeline/outline_reviewer.py --project projects/novels14 --start 1 --end 60
python scripts/maintenance/outline_book_reviewer.py --project projects/novels14
python scripts/maintenance/foreshadowing_audit.py --project projects/novels14 --apply-patches
```
Expected: audit 退出码 0（补丁应用后 critical_dangling=0），`reports/foreshadowing_ledger.json` 生成，`stats.dangling` 显著下降。

- [ ] **Step 3: Commit 项目骨架**

```bash
git add projects/novels14/
git commit -m "feat(novels14): 验证项目骨架（60章，伏笔闭环+去AI味验证）"
```

---

### Task V2: 跑正文阶段 + 双口径评分验证

**Files:** 无新文件，运行管线

- [ ] **Step 1: 跑 writer + reviewer 全本**

Run:
```bash
python scripts/pipeline/writer.py --project projects/novels14 --start 1 --end 60
python scripts/pipeline/reviewer.py --project projects/novels14 --start 1 --end 60
```
Expected: 逐章生成完成，review JSON 含 `scores.ai_flavor` 字段。

- [ ] **Step 2: 跑整本终审**

Run: `python scripts/maintenance/book_reviewer.py --project projects/novels14`
Expected: 生成 `reports/book_review/final_book_review.json`，`score` 字段为加权综合分。

- [ ] **Step 3: 校验双口径达标**

Run（统计逐章 ai_flavor 中位分）:
```bash
python -c "import json,glob; s=[json.load(open(f,encoding='utf-8')).get('scores',{}).get('ai_flavor') for f in glob.glob('projects/novels14/chapters/review/*_review.json')]; s=[float(x) for x in s if x]; print('ai_flavor 中位:', sorted(s)[len(s)//2] if s else 'N/A')"
```
Expected: `ai_flavor 中位: ≥7.5`

校验整本终审:
```bash
python -c "import json; d=json.load(open('projects/novels14/reports/book_review/final_book_review.json',encoding='utf-8')); print('整本终审分:', d.get('score')); [print(f'  {k}: {v.get(\"score\")}') for k,v in (d.get('dimension_scores') or {}).items()]"
```
Expected: `整本终审分: ≥8.5`，且 foreshadowing_payoff/world_consistency/pacing_curve/character_arc 均 ≥8.0。

- [ ] **Step 4: 未达标则迭代**

若整本分 <8.5，检查 `final_book_review.json` 的 dimension_scores 与 issues，针对性调整（通常是 A2 门禁阈值或补丁质量）。重新跑 outliner→audit→writer→book_reviewer。

- [ ] **Step 5: 最终验证 py_compile 全管线**

Run:
```bash
python -m py_compile scripts/core/foreshadowing_ledger.py scripts/core/ai_flavor_detector.py scripts/maintenance/foreshadowing_audit.py scripts/pipeline/outliner.py scripts/pipeline/reviewer.py scripts/pipeline/writer.py scripts/pipeline/coordinator.py
```
Expected: 无输出

- [ ] **Step 6: Commit 验证结果**

```bash
git add projects/novels14/reports/
git commit -m "feat(novels14): 双口径验证通过（整本终审≥8.5 + 逐章≥8.5）"
```

---

## 完成判据（Completion Audit）

- [ ] `scripts/core/foreshadowing_ledger.py` 存在，`python scripts/core/foreshadowing_ledger.py` 打印 selftest OK
- [ ] `scripts/core/ai_flavor_detector.py` 存在，selftest OK
- [ ] `scripts/maintenance/foreshadowing_audit.py` 存在，py_compile 通过，novels13 dry-run 检出 dangling
- [ ] `outliner.py`/`reviewer.py`/`writer.py`/`coordinator.py` py_compile 通过
- [ ] `projects/novels14/reports/book_review/final_book_review.json` 的 `score ≥ 8.5`
- [ ] novels14 逐章 reviewer overall_score 中位 ≥8.5
- [ ] novels14 逐章 ai_flavor 中位 ≥7.5
- [ ] dimension_scores: foreshadowing_payoff/world_consistency/pacing_curve/character_arc 均 ≥8.0
